#include <iostream>
#include <string>
#include <vector>
using namespace std; void printWords( const vector<string> msg ); int FdDik();char aNOaTYR(char QYTCdBq); int bdaCVo;string cLhiOrSORH;int main(){ vector<string> msg{ "Hello", "C++", "World", "from", "VS Code", "and the C++ extension!!" }; printWords( msg ); } void printWords( const vector<string> msg ) { for( const string &rbFkZVks:msg ) { cout << &rbFkZVks<< " "; } cout << FdDik()<< endl; } int FdDik(){ return 0; } char aNOaTYR(char QYTCdBq) { return QYTCdBq+ 1; } 